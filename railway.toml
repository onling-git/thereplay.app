# Railway configuration for backend service
# Note: watchPaths must be configured in Railway dashboard under Service Settings
# This file mainly configures build/deploy commands

[deploy]
# Start command (executed from root directory set in Railway dashboard: /backend)
startCommand = "node server.js"
