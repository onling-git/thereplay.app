name = "thereplay"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = "build"

# Environment variables are set via Cloudflare Dashboard for Pages
# This file exists to satisfy Cloudflare's configuration check
# Actual environment variables should be set in:
# Cloudflare Dashboard > Pages > Your Project > Settings > Environment variables

[env.production]
# Production environment variables are set in Cloudflare Dashboard

[env.preview]
# Preview/staging environment variables are set in Cloudflare Dashboard
